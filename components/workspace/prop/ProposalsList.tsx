import { GossipProposalInformation } from "../../../lib/util/ipfs";
/**
 * Renders a list of gossiped proposals.
 */
export const ProposalsList = ({ proposals }: { proposals: GossipProposalInformation[] }) => {
	return (
		<div>
		</div>
	);
};
